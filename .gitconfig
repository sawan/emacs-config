[user]
	name = Sawan Vithlani
	email = Sawan.Vithlani@medibankhealth.com.au

[alias]
    co = checkout
    st = status
    stat = status
    sta = stash
    dt = difftool
    mt = mergetool
    lo = log --oneline --decorate
    commit-reuse = commit -a --reuse-message=HEAD
    sdiff = diff --ignore-all-space --name-status -C -M
    stash-p = stash pop
    stash-s = stash save
    stash-l = stash list
    maspull = ! git checkout master && git pull origin master
    pusho =   push origin HEAD
    pullo =   pull origin HEAD
    mdiff = ! git sdiff master...
    show-mdiff = ! git sdiff master... | sed -e 's/^[MAD]//' | git dt master...

[color]
    ui = auto

[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green

[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold

[color "status"]
    added = yellow
    changed = green bold
    untracked = cyan
